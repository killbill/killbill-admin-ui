<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li <%= "class=active" if params[:controller].ends_with?('accounts') %>><%= link_to 'Account', kaui_engine.account_path(@account.account_id) %></li>
        <li <%= "class=active" if params[:controller].ends_with?('bundles') %>><%= link_to 'Subscriptions', kaui_engine.account_bundles_path(@account.account_id) %></li>
        <li <%= "class=active" if params[:controller].ends_with?('invoices') %>><%= link_to 'Invoices', kaui_engine.account_invoices_path(@account.account_id) %></li>
      </ul>
    </div>
  </div>
</nav>
